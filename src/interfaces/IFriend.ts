export interface IFriend {
  friendId: string;
  senderId: string;
  senderEmail: string;
  senderFullName: string;
  receiverId: string;
  receiverEmail: string;
  receiverFullName: string;
  status: string;
  sentAt: string;
}
